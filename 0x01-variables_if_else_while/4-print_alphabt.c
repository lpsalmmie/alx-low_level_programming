#include <stdio.h>
#include <stdlib.h>

/**
 * main - returns alphabets both except e and q
 *
 * Retun: Always 0 (Success)
 */

int main(void)
{
	char c;

	c = 'a';

	while
		(ch <= 'z')
	{
		if ((ch != 'q' && c != 'e') && c <= 'z')
			putchar(c);
		c++;
	}
	putchar('\n');
	return (0);
}
